[project]
name = "a2a-server"
version = "0.1.0"
description = "Minimal A2A agent: thank-you status and wallet-address artifact"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.124.4",
    "sse-starlette>=3.0.3",
    "uvicorn>=0.38.0",
    "python-dotenv>=1.2.1",
    "a2a-sdk>=1.0.1",
]
